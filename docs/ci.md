# CI Checks

Suggested minimal checks for this repo:

- Schema validation: run schemas/validate.ps1
- Markdown lint (optional): basic formatting checks
- Broken link check (optional): verify docs references
- Go tests: run go test ./... in repo root
- Go 1.21+ is required for go test ./....

